"""Reserved: base contract for an agent skill.

Intended to mirror the shape of `providers.base_provider.BaseProvider` —
an abstract base class that concrete, invokable agent capabilities will
implement. See docs/ROADMAP.md.
"""
