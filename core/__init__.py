"""Core application/domain layer.

Contains the AI engine, session state, message/response models, and the
runtime architecture foundation. This layer depends on `providers/` but
must never depend on `ui/`.
"""
