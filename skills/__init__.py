"""Reserved package for the agent skills system.

Will eventually define a `Skill` contract and a `SkillManager` registry,
mirroring the shape of `providers/base_provider.py` and
`providers/provider_manager.py`. See docs/ROADMAP.md.
"""
