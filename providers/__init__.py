"""AI provider implementations.

Every provider implements the `BaseProvider` contract defined in
`providers/base_provider.py`. `ProviderManager` is the single factory used
to instantiate a provider by name.
"""
